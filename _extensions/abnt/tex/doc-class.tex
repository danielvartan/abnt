\documentclass[
$if(fontsize)$
$fontsize$,
$else$
12pt,
$endif$
openright,
$if(sides)$
$sides$,
$else$
oneside,
$endif$
$if(papersize)$
$papersize$paper,
$else$
a4paper,
$endif$
$if(section-options)$
$for(section-options)$
$section-options$$sep$,
$endfor$,
$else$
chapter=TITLE,
section=TITLE,
$endif$
$if(other-babel-lang)$
$for(other-babel-lang)$
$other-babel-lang$$sep$,
$endfor$,
$else$
french,
spanish,
brazil,
$endif$
$if(babel-lang)$
$babel-lang$
$else$
english
$endif$
]{$documentclass$}
