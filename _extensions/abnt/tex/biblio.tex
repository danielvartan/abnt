% Set `biblatex` Options -----

\usepackage[
$for(biblatexoptions)$,$biblatexoptions$$endfor$
]{biblatex}

\usepackage{csquotes}

% Set Bibliography Resource -----

$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$

% Set Reference Formatting -----

\AtBeginBibliography{\vspace{0.5\baselineskip}}
\AtEveryBibitem{\clearfield{annotation}}
\renewcommand{\bibfont}{\ABNTEXfontereduzida}

$if(bibhang)$
\setlength{\bibhang}{$bibhang$}
$endif$

$if(bibparsep)$
\setlength{\bibparsep}{$bibparsep$}
$endif$

% Set Footnote -----

$if(biblio-footnote)$
\newcommand{\bibnamewithfootnote}{
  \MakeUppercase{\newbibname}\protect\footnote{$biblio-footnote$}
}
$endif$

$if(nocite-ids)$
\nocite{$for(nocite-ids)$$it$$sep$, $endfor$}
$endif$

\defbibheading{bibheading}[$if(biblio-footnote)$\bibnamewithfootnote$else$\MakeUppercase{\newbibname}$endif$]{
  \ifthenelse{\boolean{ABNTEXupperchapter}}{
    \setboolean{ABNTEXupperchapter}{false}
    \chapter*{#1}
    \markboth{#1}{#1}
    \setboolean{ABNTEXupperchapter}{true}
  }{
    \chapter*{#1}
    \markboth{#1}{#1}
  }
}
