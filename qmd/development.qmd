# [Showcase] Development {#sec-development}

```{r}
#| label: Setup
#| include: false

library(here)

here("R", ".setup.R") |> source()
```

```{r}
#| echo: false
#| output: asis

library(cli)
library(withr)

with_seed(
  seed = 4,
  code = {
    cli:::lorem_ipsum(2) |> cat(sep = "\n\n")
  }
)
```

## Secondary Section

```{r}
#| echo: false
#| output: asis

library(cli)
library(withr)

with_seed(
  seed = 5,
  code = {
    cli:::lorem_ipsum(2) |> cat(sep = "\n\n")
  }
)
```
