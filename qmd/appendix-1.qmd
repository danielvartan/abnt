# [Showcase] {#sec-appendix-a}

```{r}
#| label: Setup
#| include: false

library(here)

here("R", ".setup.R") |> source()
```

```{r}
#| echo: false
#| output: asis

library(cli)
library(withr)

with_seed(
  seed = 8,
  code = {
    cli:::lorem_ipsum(2) |> cat(sep = "\n\n")
  }
)
```

## Secondary Section

```{r}
#| echo: false
#| output: asis

library(cli)
library(withr)

with_seed(
  seed = 9,
  code = {
    cli:::lorem_ipsum(1) |> cat(sep = "\n\n")
  }
)
```

```{=latex}
\end{apendicesenv}
```

<!-- appendices end -->

```{=latex}
\begin{anexosenv}
```
